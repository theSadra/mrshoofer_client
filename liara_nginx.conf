# Liara Nginx Configuration
# This file disables WAF and rate limiting for ORS routes

location /ORS {
    # Disable WAF for ORS routes
    modsecurity off;
    
    # Disable rate limiting
    limit_req off;
    limit_conn off;
    
    # Pass to Next.js
    proxy_pass http://localhost:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

location /ors {
    # Disable WAF for ORS routes
    modsecurity off;
    
    # Disable rate limiting
    limit_req off;
    limit_conn off;
    
    # Pass to Next.js
    proxy_pass http://localhost:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
