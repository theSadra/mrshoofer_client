
═══════════════════════════════════════════════════════════════════════
                    WHY /ORS/api/trip RETURNS 403
═══════════════════════════════════════════════════════════════════════

CURRENT FLOW (BLOCKED):
┌────────────┐
│   Client   │
│  (POST)    │
└──────┬─────┘
       │ Authorization: Bearer TOKEN
       │ POST /ORS/api/trip
       ▼
┌─────────────────────────────────────────────────────────────────┐
│                      LIARA PROXY/WAF                            │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  1. Check request pattern                                │ │
│  │  2. POST to /ORS/api/trip looks "suspicious"             │ │
│  │  3. WAF rules triggered                                  │ │
│  │  4. BLOCK REQUEST ❌                                      │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Returns:                                                       │
│    - Status: 403 Forbidden                                     │
│    - Server: Liara                                             │
│    - Body: empty (0 bytes)                                     │
│    - Never reaches your app!                                   │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼
   ❌ 403 RETURNED TO CLIENT
   (Your Next.js app never sees the request!)


WHY OTHER ENDPOINTS WORK:
┌────────────────────────────────────────────────────────────────┐
│ Endpoint              Method    Status    Why?                 │
├────────────────────────────────────────────────────────────────┤
│ /ORS/api/debug        GET       ✅ 200    GET allowed by WAF   │
│ /ORS/api/test         GET       ✅ 200    GET allowed by WAF   │
│ /ORS/api/register     GET       ✅ 200    GET allowed by WAF   │
│ /ORS/api/trip         POST      ❌ 403    POST blocked by WAF  │
└────────────────────────────────────────────────────────────────┘


AFTER DEPLOYMENT (FIXED):
┌────────────┐
│   Client   │
│  (POST)    │
└──────┬─────┘
       │ Authorization: Bearer TOKEN
       │ POST /ORS/api/trip
       ▼
┌─────────────────────────────────────────────────────────────────┐
│                      LIARA PROXY/WAF                            │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  1. Check if path matches /ORS/*                         │ │
│  │  2. YES → Bypass WAF (liara_nginx.conf)                  │ │
│  │  3. WAF disabled for this route                          │ │
│  │  4. ALLOW REQUEST ✅                                      │ │
│  └──────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    NEXT.JS APP (PORT 3000)                      │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  middleware.ts                                           │ │
│  │  • Check: Is path /ORS/* ?                               │ │
│  │  • YES → Bypass NextAuth                                 │ │
│  │  • Continue to route handler                             │ │
│  └──────────────────────────────────────────────────────────┘ │
│                          ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  /ORS/api/trip/route.tsx                                 │ │
│  │  • requireORSAuth() checks Bearer token                  │ │
│  │  • Token valid → Process request                         │ │
│  │  • Create trip in database                               │ │
│  │  • Return 201 Created ✅                                  │ │
│  └──────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼
   ✅ 201 CREATED
   (Request successfully processed!)


SOLUTION FILES CREATED:
═══════════════════════════════════════════════════════════════════

1. liara.json
   ├─ waf: { enabled: false }
   └─ rateLimit: { enabled: false }

2. liara_nginx.conf
   ├─ location ~ ^/[Oo][Rr][Ss]
   ├─ set $waf_enabled 0
   ├─ limit_req off
   └─ limit_conn off

3. LIARA-WAF-FIX.md
   └─ Complete documentation and troubleshooting

4. deploy-waf-fix.ps1
   └─ Automated deployment script


DEPLOY COMMAND:
═══════════════════════════════════════════════════════════════════

  .\deploy-waf-fix.ps1

  OR manually:
  liara deploy --app mrshoofer-client --port 3000


VERIFY AFTER DEPLOYMENT:
═══════════════════════════════════════════════════════════════════

  node test-rate-limiting.js

  Expected: All 20 requests should return 200/201/400
            (NOT 403!)


IF STILL 403 AFTER DEPLOYMENT:
═══════════════════════════════════════════════════════════════════

  Liara's WAF settings might need manual configuration:

  1. Go to: https://console.liara.ir/
  2. Select: mrshoofer-client
  3. Navigate: Settings → Security
  4. Action: Disable WAF OR add /ORS/* to whitelist
  5. Contact support: support@liara.ir


KEY TAKEAWAYS:
═══════════════════════════════════════════════════════════════════

✅ Your code is correct - middleware, auth, routes all work
✅ Authentication systems are properly separated
✅ The issue is Liara's infrastructure, not your application
❌ WAF is blocking POST requests before they reach your app
🔧 Solution: Disable WAF or whitelist /ORS/* paths
